# The Lokmaco · QR Menu — контекст проекта

QR-меню для кофейни-кондитерской The Lokmaco (Фергана, турецкие десерты: локма, вафли, кофе). Хостинг — Vercel.

## Архитектура

- Next.js 16 (App Router, TS). Меню без БД; бонусная система — через iiko Card API.
- Меню — `data/menu.json`, фото — `public/uploads/`.
- Роутинг: `/` — старое фото-меню (из `origin/main`), `/test-menu` — новое QR-меню, `/admin` — админка, `/card` — бонусная карта клиента.
- Публичное меню `/test-menu`: RU/UZ/EN, welcome-лоадер, секции Еда/Напитки, категории-чипсы, поиск, диалог блюда с КБЖУ. Всё в `components/MenuApp.tsx`.
- Админка `/admin` (`components/AdminApp.tsx`): пароль из `ADMIN_PASSWORD`, CRUD категорий/блюд, три языка полей, загрузка фото (ресайз до 900px JPEG на клиенте).
- Сохранение: локально — запись в файлы; на Vercel — коммит в GitHub через API (`lib/store.ts`, env: `GITHUB_TOKEN`, `GITHUB_REPO`, `GITHUB_BRANCH`) → автопересборка.
- Палитра: эспрессо `#3B2416` + мёд `#E0A83E` + фисташка `#7FA653` + тёплый крем `#FAF3E7` (переменные в `app/globals.css`). Шрифты: Playfair Display + Manrope.
- Бонусная система: iiko Card API (токен от поддержки iiko). Кешбэк по сумме чека: <400к — 3%, ≥400к — 5%, ≥700к — 7%. Списание бонусов — макс 50% от чека. Клиент сам говорит сколько списать.

## Сделано 2026-07-14

- Созвон с поддержкой iiko: бонусная система будет через iiko Card (лицензия не нужна, входит в текущую подписку). Через API можно получать баланс, создавать клиентов, проводить оплату бонусами. Асиль получит токен.
- Решение: Supabase для бонусов больше не нужен — всё через iiko Card API.
- Google Wallet: пока отложен (на iOS нет Google Wallet; Apple Wallet — платный $99/год). Бесплатная альтернатива — PWA «Добавить на экран».
- Force-push `qr-menu` в `origin/main` для деплоя на Vercel. Добавлен `vercel.json` с `framework: nextjs`.
- Старое фото-меню (`components/OldMenu.tsx`) на `/`, новое QR-меню перенесено на `/test-menu`.
- Убран блок с количеством позиций, временем работы и обслуживания из нового меню `MenuApp` по просьбе Асиля.


## Сделано 2026-07-11

- Публичное меню стало "премиальнее": липкая панель управления, краткий блок с количеством позиций/часами/сервисом, быстрые фильтры `Все вкусы / Хит / Новинка`.
- Добавлен блок "С чего начать" с подборкой Lokmaco из хитов/новинок без новых полей в `menu.json`.
- В диалоге блюда добавлены "Похожие вкусы" из той же категории; переключение между похожими блюдами работает внутри открытого диалога.

## Сделано 2026-07-09

- Реальное меню из CDR-макетов: 93 позиции, 15 категорий, 33 фото блюд.
- CDR = ZIP; растры в content/data/Bitmaps.dat в сыром CMYK — извлечены и сконвертированы скриптом (numpy).
- Цены из макета; спорные места: раскладка цен локмы по размерам, боксы мороженого, фреши — Асиль должен проверить в админке.

## Сделано 2026-07-08

- Бейджи блюд (хит/новинка/острое): `badges` в item, чекбоксы в админке, переводятся на 3 языка.
- Инфо-блок в футере: адрес, телефон (tel:), Instagram, Wi-Fi — поля в карточке «Бренд» админки, пустые скрываются.
- Заказы/корзина НЕ нужны — решение Асиля, не предлагать.

## Сделано 2026-07-07

- Каркас проекта, публичное меню, админка, API (`/api/admin/menu`, `/api/admin/upload`), README с инструкцией деплоя.
- Проверено локально: рендер, языки, диалог, логин, правка цены → запись в menu.json, аплоад фото, 401 на неверный пароль.

## Git

Репозиторий: https://github.com/asilJPG/lockmaco-menu, код в ветке `qr-menu`. `origin/main` = force-push из `qr-menu` (для Vercel).

## Осталось

- Получить токен iiko Card API от поддержки, изучить документацию, интегрировать бонусы в `/card`.
- Настроить env-переменные на Vercel (`ADMIN_PASSWORD`, `GITHUB_REPO`, `GITHUB_BRANCH`, iiko API токен).
- Опционально: Google Wallet, PWA для iOS, логотип-картинка, QR-коды на столы.
